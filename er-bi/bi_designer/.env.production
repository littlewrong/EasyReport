# public base path for nginx sub-path deployment
VITE_BASE = '/bidesigner/'

# API path remains same-origin /biapi and is proxied by nginx
VITE_API_BASE = '/biapi'

# Keep generic request base at root so custom chart URLs like /mock are not rewritten.
VITE_PRO_PATH = '/'
